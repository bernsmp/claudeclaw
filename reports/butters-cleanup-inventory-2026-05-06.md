# Butters Cleanup Inventory — 2026-05-06

Scope: `/Users/maxb/Desktop/max-command-center/butters`

This inventory treats Butters as its own repository. The MCC root `butters` gitlink is intentionally not updated in this cleanup pass.

## Repository State At Start

- Butters repo: `main` at `03a2023`
- Remote: `https://github.com/earlyaidopters/claudeclaw`
- MCC root gitlink recorded: `ac0609f`
- MCC root showed: `M butters`
- Butters dirty set: 25 tracked modified files plus 200+ untracked paths before ignore cleanup

## Classification

### KEEP

Real Butters source/docs/config work to preserve in Butters commits:

- Hermes/shadow backend support: `.env.example`, `package.json`, `package-lock.json`, `src/agent.ts`, `src/agent-types.ts`, `src/claude-runner.ts`, `src/hermes-runner.ts`, `src/config.ts`, `src/env.ts`, `src/orchestrator.ts`
- LCM memory work: `src/lcm/**`, `src/index.ts`, `src/memory.ts`, `src/bot.ts`, `src/db.ts`, `src/db.test.ts`, `src/memory.test.ts`
- Dashboard/bot/runtime hardening: `src/dashboard.ts`, `src/dashboard-html.ts`, `src/scheduler.ts`, `src/schedule-cli.ts`, `src/planner.ts`, `scripts/wa-daemon.ts`, `scripts/precall-check.sh`, `scripts/reconcile-phase1-scheduler.cjs`
- Runtime reference docs: `docs/runtime/active-clients.md`, `docs/runtime/ai-radar-reference.md`, `docs/runtime/capabilities-reference.md`, `docs/runtime/scheduler-reference.md`
- Butters agent network: `agents/SCHEMAS.md`, untracked `agents/am-*`, `agents/pm`, `agents/compound-extractor`, `agents/content-intelligence`, `agents/pattern-archaeologist`, `agents/strategic-intelligence`
- Butters operational scripts: `scripts/add-*.cjs`, `scripts/*.{mjs,js,sh,py}` that operate on Butters DB, Telegram, content queue, PM polling, Typefully, and AI radar
- Strategic chief of staff skill: `skills/strategic-chief-of-staff/**`, `skills/strategic-chief-of-staff.skill`
- Operator docs: `AGENTS.md`, `TROUBLESHOOTING.md`, `THE-OPERATOR-SPEC.md`, `OPERATOR-VOICE-PATTERNS.md`, `MAX-SOCIAL-POST-GENERATOR.md`
- Butters architecture image: `butters-architecture-whiteboard.png`
- Repo hygiene/docs: `.gitignore`, `README.md`, `CLAUDE.md`, `REBUILD_PROMPT.md`, this report

### IGNORE

Local/generated/runtime artifacts that should stay out of commits:

- Top-level SQLite/runtime DB files: `butters.db`, `claudeclaw.db`, plus future `*.db`, `*.sqlite*`, WAL/SHM files
- Local agent skill mirror folders: `.adal/`, `.augment/`, `.bob/`, `.codebuddy/`, `.commandcode/`, `.continue/`, `.cortex/`, `.crush/`, `.factory/`, `.goose/`, `.iflow/`, `.junie/`, `.kilocode/`, `.kiro/`, `.kode/`, `.mcpjam/`, `.mux/`, `.neovate/`, `.openhands/`, `.pi/`, `.pochi/`, `.qoder/`, `.qwen/`, `.roo/`, `.trae/`, `.vibe/`, `.windsurf/`, `.zencoder/`
- Canonical local skill source generated by installer: `.agents/`
- Local Claude skill/log mirrors: `.claude/skills/`, `.claude/claudex/`
- Local editor debug context: `.cursor/`
- Generated skill installer lock: `skills-lock.json`
- Symlinked Higgsfield skill mirrors under `skills/higgsfield-*`
- Local/generated media and scratch draft: `*.mp4`, `x-profile-draft.md`
- Local Node pin: `.nvmrc` currently says Node `22`; keep ignored until the supported Butters runtime is decided explicitly

### RESTORE

No tracked file is marked RESTORE in this pass. The tracked diffs were preserved or left for review rather than reverted.

### MOVE

Candidates to move later, only with approval:

- `butters-architecture-whiteboard.png` may belong in `assets/` or a design/reference folder, but is preserved as-is in this pass.
- Top-level operator/content docs may later move into a dedicated `docs/operator/` or `docs/content/` folder if repo organization becomes a follow-up task.

### REVIEW

Classified but not deleted:

- `.nvmrc` remains on disk but ignored because Butters has had Node ABI drift before.
- Existing generated `dist/` is ignored and not part of this cleanup.

## Safety Notes

- No `git reset --hard` used.
- No `git clean -fd` used.
- No untracked folder deleted.
- No MCC root gitlink update made.
- Local DBs, secrets, runtime state, and generated mirrors are intentionally not staged.

## Verification

- `git status --short --untracked-files=all` reviewed before staging.
- `git diff --name-status` reviewed before staging.
- `npm run typecheck` passed.
- First `npm test` exposed local `better-sqlite3` Node ABI drift (`NODE_MODULE_VERSION 137` installed, `141` required by current Node). Ran `npm rebuild better-sqlite3`.
- `npm test` passed after rebuild: 9 test files, 131 tests.
